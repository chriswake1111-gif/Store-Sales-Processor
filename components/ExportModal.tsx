// This component is no longer used as we leverage the browser's native File System API for a better user experience.
import React from 'react';
const ExportModal = () => null;
export default ExportModal;